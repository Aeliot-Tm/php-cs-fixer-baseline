services:
  php-cli:
    build:
      context: ./
      dockerfile: .infra/docker/php/Dockerfile
    expose:
      - '9003'
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - .:/app
